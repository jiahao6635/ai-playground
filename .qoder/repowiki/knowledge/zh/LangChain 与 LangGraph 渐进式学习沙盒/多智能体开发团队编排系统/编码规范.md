- 所有智能体子图均遵循“构建函数返回编译后图对象”的模式（如 `build_pm_subgraph`），确保主图集成时的接口一致性。
- 智能体节点函数统一接收 `state: dict` 并返回包含特定状态键（如 `requirements` 或 `code`）及 `messages` 增量的字典，以驱动 LangGraph 的状态更新。
- 在涉及代码执行的场景（如 `tester_agent.py` 和 `tools.py`）中，均采用白名单机制限制 `__builtins__` 并拦截危险模块导入，以确保沙箱环境的安全性。