- **入口脚本**：`main.py` 负责基础翻译与交互式演示，`fewshot.py` 专注于 Few-shot 提示效果对比。
- **数据模型层**：`models.py` 定义 `TranslationResult` 等 Pydantic 模型，作为 `llm.with_structured_output()` 的契约。
- **依赖方向**：模块通过 `sys.path` 注入引用根目录下的 `common` 包（LLM 实例、配置、工具函数），内部无复杂分层，采用脚本直连 LLM 的扁平架构。