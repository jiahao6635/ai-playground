- 使用 LCEL 管道操作符 `|` 串联 Prompt、LLM 和 Output Parser，形成声明式调用链。
- 通过 `ChatPromptTemplate.from_messages()` 构建包含 system 和 human 角色的消息模板，支持变量插值。
- 利用 `llm.with_structured_output(Model)` 替代传统解析逻辑，直接获取类型安全的 Pydantic 对象。
- 在 `fewshot.py` 中通过 `FewShotChatMessagePromptTemplate` 将示例字典动态展开为聊天消息序列。