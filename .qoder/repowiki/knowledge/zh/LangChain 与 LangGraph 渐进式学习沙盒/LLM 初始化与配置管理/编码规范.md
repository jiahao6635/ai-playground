- 采用 dataclass 定义配置结构（LLMConfig, EmbeddingConfig），并通过 getter 函数封装环境变量读取逻辑，提供默认值和缺失校验。
- LLM 实例化采用工厂模式（get_llm, get_embeddings），隐藏底层 ChatOpenAI/OpenAIEmbeddings 的具体参数配置细节。
- 在 utils.py 中通过 os.path 动态计算项目根目录并修改 sys.path，实现无需安装包的本地模块引用机制。