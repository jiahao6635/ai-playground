- 在 graph 节点中调用 `interrupt(value)` 传递上下文数据，并在外部通过 `Command.resume(result)` 注入决策结果以恢复流程。
- 在执行动态代码前实施安全沙箱策略，通过白名单限制 `__builtins__` 并拦截危险系统调用（如 os, subprocess）。
- 所有文件均通过 `sys.path.insert` 动态添加项目根目录至搜索路径，以支持从子模块导入 `common` 包。