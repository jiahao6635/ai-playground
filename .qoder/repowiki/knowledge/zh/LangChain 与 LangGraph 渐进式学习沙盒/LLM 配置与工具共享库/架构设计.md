- **分层结构**：模块内部划分为配置层 (`config.py`)、工厂层 (`llm.py`) 和工具层 (`utils.py`)。
- **依赖方向**：`llm.py` 依赖 `config.py` 获取类型安全的配置对象；`config.py` 直接依赖 `dotenv` 读取环境变量。
- **入口点**：`__init__.py` 声明模块为公共共享库，供子项目通过 `from common.xxx import yyy` 引用。
- **路径处理**：`utils.py` 在导入时自动将项目根目录注入 `sys.path`，确保跨子项目的模块解析能力。