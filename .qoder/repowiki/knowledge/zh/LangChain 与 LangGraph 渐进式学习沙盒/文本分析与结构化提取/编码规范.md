- 统一使用 `ChatPromptTemplate.from_messages` 配合 system/human 角色模板来定义提示词结构。
- 普遍采用 `prompt | llm | StrOutputParser()` 或 `prompt | structured_llm` 的管道模式组装执行链。
- 在处理多字段输入时，习惯引入 `operator.itemgetter` 从字典中提取特定键值传递给子链。