- **分层结构**：模块划分为数据摄入层（`ingest.py`）、检索配置层（`retriever.py`）与应用交互层（`main.py`），通过共享的 `vectorstore` 目录解耦索引构建与查询逻辑。
- **核心组件**：`models.py` 定义结构化响应模型；`retriever.py` 封装 FAISS 加载与检索器工厂方法，支持相似度与 MMR 策略切换。
- **依赖方向**：各脚本均依赖根目录下的 `common` 模块获取 LLM 实例与 Embedding 配置，遵循自底向上的数据流：原始文本 → 分块向量化 → FAISS 索引 → LCEL 检索链。